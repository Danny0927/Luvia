import { Text, View } from "react-native";

export default function StepsScreen() {
  return (
    <View style={{
      flex: 1,
      backgroundColor: "#FFF9E3",
      justifyContent: "center",
      alignItems: "center"
    }}>
      <Text>Steps tracker</Text>
    </View>
  );
}

